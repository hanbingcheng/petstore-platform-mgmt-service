<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.petstore.mgmt.mapper.CategoryMapper">

    <resultMap id="categoryResultMap" type="com.example.petstore.mgmt.entity.CategoryEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="categoryResultMap">
        SELECT * FROM categories ORDER BY name
    </select>
    <select id="findById" resultMap="categoryResultMap">
        SELECT * FROM categories WHERE id = #{id}
    </select>
    <select id="findByName" resultMap="categoryResultMap">
        SELECT * FROM categories WHERE name = #{name}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO categories (name, description, created_at, updated_at)
        VALUES (#{name}, #{description}, NOW(), NOW())
    </insert>
    <update id="update">
        UPDATE categories SET name = #{name}, description = #{description}, updated_at = NOW()
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM categories WHERE id = #{id}
    </delete>
</mapper>
